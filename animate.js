
//This function is meant to animate a line of text from left to right beneath the h1 of parallax at the beginnning of the project. It is currently not working, and the attached elements are commented out

// function myMove() {
//     var elem = document.getElementById("animation"); // select the html element with id 'myAnimation' (the moving square)
//     var pos = 0; // set initial position to 0
//     var id = setInterval(frame, 10); // creates a timer that will call the function 'frame' every 10 miliseconds until we stop it
//     function frame() { // define the 'frame' function
//         if (pos == 550) { // check if position is 350, then end of the container
//             clearInterval(id); // if true, stop the timer, animation has completed
//         } else { 
//             pos++; // increase the position variable from line 3
//             elem.style.left = pos + 'px'; // change the style/css left of the Parallax text possition
//         }
//     }
// }






